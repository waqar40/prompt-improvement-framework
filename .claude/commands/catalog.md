---
description: "List everything in this repo — every command, skill, and agent with its usage, inputs, and outcomes."
argument-hint: "(no arguments)"
allowed-tools: Read, Glob, Bash
---

Read `.claude/skills/catalog/SKILL.md` and follow it exactly: reconcile the catalog against the
`.claude/` directories on disk, then print the full capability guide (Pipeline, Commands,
Skills, Agents) with usage, input, and outcome for each.

$ARGUMENTS
